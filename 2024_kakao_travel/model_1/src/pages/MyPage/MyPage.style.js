import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
`;

const TopBox = styled.div``;
const MainBox = styled.div``;
const MainUpBox = styled.div``;
const MainDownBox = styled.div``;

export { Container, TopBox, MainBox, MainUpBox, MainDownBox };
